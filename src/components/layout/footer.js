import React from "react";
import styled from "styled-components";

const StyledFooter = styled.footer `
    background-color: #fbfbfb;
    padding: 40px 0px;
`;

const Footer = ()=> {
    return <StyledFooter>This is footer</StyledFooter>;
};

export default Footer;
